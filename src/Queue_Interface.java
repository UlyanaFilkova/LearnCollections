public class Queue_Interface {
    /*
        QUEUE
    Не хранит null
    element(), peek() - верхний элемент
    remove(), poll() - удаляет верхний элемент. Если очередь пуста, то remove выдаст Exception, poll - вернет null
    remove(obj) - удаляет указанный элемент (в любом месте очереди)

    add() - добавление
    offer() - попытка добавить в конец (мб из-за размера очереди нельзя)

        DEQUE - интерфейс
    addFirst()
    addLast()
    getFirst()
    getLast()

    offerFirst(), push()
    offerLast() - false, если очередь с ограниченной емкостью заполнена
    pop(), pollFirst(), removeFirst()
    pollLast(), removeLast() - null, если очередь пуста
    peekFirst()
    peekLast()
    removeLastOccurrence(Object obj)
    removeFirstOccurrence(Object obj)

        ARRAYDEQUE - класс

        LINKEDLIST - класс,
    двусвязный список, имеет индексы и итератор

        PRIORITYQUEUE
    По умолчанию очередь с приоритетами размещает элементы согласно порядку сортировки Comparable.
    Элементу с наименьшим значением присваивается наибольший приоритет.
    Если несколько элементов имеют одинаковый наивысший элемент – рандомно.
    Также можно указать специальный порядок размещения, используя Comparator.



    */

}
